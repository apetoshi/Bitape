'use client';

import { ViewModeToggle as VMT, ViewMode } from './ViewModeToggle';

// Re-export the component and types
export type { ViewMode };
export const ViewModeToggle = VMT;

export default ViewModeToggle; 